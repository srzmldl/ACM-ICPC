#include <bits/stdc++.h>
#define REP(i, n) for(int i = 0; i < n; ++i)
#define REPP(i, a, b) for(int i = (int) (a); i <= (int) (b); ++i)
#define MST(a, b) memset((a), (b), sizeof(a))

using namespace std;

int main()
{
	freopen("zoj3613.txt", "r", stdin);
	
	return 0;
}
