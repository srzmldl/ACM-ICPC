#include <cstring>
#include <cstdlib>
#include <cstdio>
#include <iostream>
#define MAXN 1123

using namespace std;

int main()
{
    freopen("in.txt", "r", stdin);
    int n, c;
    const int INT = 1010;
    for (; scanf("%d%d", &n, &c) != EOF; )
    {
        for (int i = 1; i <= n; ++i)
            scanf("%d", &p[i]);
        int mx = -INF, mi = INF;
        for (int i = 1; i <= n; ++i)
        {
            scanf("%d", &q[i]);
            mx = max()
        }
    }
    return 0;
}
