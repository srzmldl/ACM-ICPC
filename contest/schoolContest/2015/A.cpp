#include <bits/stdc++.h>
#define MAXN 1123456
#define MST(a, b) memset(a, b, sizeof(a));

using namespace std;
typedef int arrayN[MAXN];


int main()
{
    freopen("A.in", "r", stdin);
    int T;
    scanf("%d", &T);
    for (int t = 1; t <= T; ++t)
    {
    }
    return 0;
}
