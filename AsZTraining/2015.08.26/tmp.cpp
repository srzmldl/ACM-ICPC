#include <bits/stdc++.h>
#define MAXN 

using namespace std;


int main()
{
	freopen("in.txt", "r", stdin);
    int n, d1, d2;
    for (;scanf("%d%d%d", &n, &d1, &d2) != EOF;)
    {
        for (int i = 1; i <= n; ++i)
            scanf("%d", a + i);
    }
	return 0;
}
